# Feature requests

- the default year in dropdown is not correct on refresh (shows 2023, should 2026). This should be automatically updated to this year and month.

- CalDAV integration for Mailbox.org (and other CalDAV providers):
  - Full read/write access (create, edit, delete events)
  - Auth via username + password or app-specific password (stored server-side, no OAuth)
  - CalDAV server: https://dav.mailbox.org
  - Calendar discovery + multi-calendar selection (like the Google multi-calendar feature)
  - Credentials stored securely in data/ (never exposed to the client)
