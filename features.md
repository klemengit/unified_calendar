# Feature requests

- [x] When refreshing the page, first show events from cache, then sync.
- [x] Always cache the events in the background (even for far off events). Then update them on sync. A priority queue can be defined (first update the current events, then later, if updating takes a long time. I don't know exactly how it owrks.)
- [x] show last synced
- [x] add week number
- [x] add dropdown to jump to year/month
- [x] set sync interval
